# HUD (overlay_reward)

Dossier réservé au HUD spécifique à l'overlay `overlay_reward`.
Ne pas supprimer — contient les scripts d'UI pour les récompenses et overlay.

Contient `.gitkeep` pour garder le dossier dans Git.
